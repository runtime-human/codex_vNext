# PH-00 managed-worktree fixture

`.env.ph00` is synthetic, ignored by Git, and listed in the root
`.worktreeinclude`. It exists only to verify Codex Desktop managed-worktree
copy behavior. It contains no secret.
