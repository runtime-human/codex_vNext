# P13 cleanup and degraded behavior

Date: 2026-09-06

## Result

- `codex plugin list`: no marketplace plugins found after removal.
- `codex plugin marketplace list`: no plugin marketplaces in scope.
- Probe `PLUGIN_DATA` directory removed after exact-path verification.
- Temporary project roles `ph00-reader`, `ph00-reviewer`, and `ph00-worker`
  removed.
- No temporary PH-00 worktree was created.
- Durable source, fixtures, evidence, ADRs, and report were preserved.
- Ordinary Codex remains available; final check reports CLI `0.153.4`.

The current Codex Desktop process still holds cache version `0.1.3` open;
`0.1.7` was removed. Exact-path recursive removal of `0.1.3` returned Windows
sharing error 32. The plugin and marketplace are no longer configured, so this
inert cache directory is the only cleanup residue. The process was not
terminated or forced because that would disrupt the active task.

## Degraded/security observations

- Plugin disabled/MCP unavailable: ordinary CLI remains usable and reports no
  installed marketplace plugin.
- Hooks failing while trusted: the deterministic MCP core and its tests remain
  usable; hooks are not treated as a semantic event bus.
- Native permission ceiling: no instruction or fallback widened sandbox or
  user permissions.
- Worker failure/degradation: unavailable nested delegation returned a bounded
  result; the parent remained usable and no retry loop was introduced.

The removed probe data was disposable and can be regenerated from the retained
source and tests; it was not moved to trash.
